version: '3.5'
services:
  users:
    build:
      context: ../../
      dockerfile: Dockerfile
    restart: always
    env_file:
      - .env
    command: npm run start:prod
    networks:
      - backend
      
networks:
  backend:
    external:
      name: cartografia-network